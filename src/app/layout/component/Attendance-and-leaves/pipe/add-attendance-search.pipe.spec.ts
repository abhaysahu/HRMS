import { AddAttendanceSearchPipe } from './add-attendance-search.pipe';

describe('AddAttendanceSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AddAttendanceSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
