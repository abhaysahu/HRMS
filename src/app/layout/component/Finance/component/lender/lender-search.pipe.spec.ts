import { LenderSearchPipe } from './lender-search.pipe';

describe('LenderSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new LenderSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
